apiVersion: v1
kind: Service
metadata:
  name: journal-app
  namespace: journal
spec:
  type: NodePort
  selector:
    app: journal-app
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
